# assets/private

Drop your own media files (video, audio, images) here to run avio locally during
development. Everything in this directory is git-ignored except this `README.md` and
`.gitkeep`, so your private files are never committed.
